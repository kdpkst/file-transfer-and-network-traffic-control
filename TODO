For file_transfer:
1. modify arguments in client.py. Orginally, they are --ip, --port... replace them with short form like -p, -ip.
2. correct some strange sentences in server.py ("key" {key}-- should be key "{key}")
3. add functions file download, file delete, etc if time permits